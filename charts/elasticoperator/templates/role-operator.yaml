---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Release.Name}}-{{ .Values.Operator.Role }}
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups:
        - ""
    resources:
      - configmaps
    verbs: ["get" ]
