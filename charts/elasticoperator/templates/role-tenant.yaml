---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Release.Name}}-tenant-{{ .Values.Operator.Role }}
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups:
        - ""
        #- apps
        #- autoscaling
        #- batch
        #- extensions
        #- policy
        #- rbac.authorization.k8s.io
        - elastic.elastic-operator
    resources:
      #- pods
      #- componentstatuses
      #- configmaps
      #- daemonsets
      #- deployments
      #- events
      #- endpoints
      #- horizontalpodautoscalers
      #- ingress
      #- jobs
      #- limitranges
      #- namespaces
      #- nodes
      #- pods
      #- persistentvolumes
      #- persistentvolumeclaims
      #- resourcequotas
      #- replicasets
      #- replicationcontrollers
      #- serviceaccounts
      #- services
      - secrets
      - indextemplates
      - kibanaspaces
      - ilms
      - slms
      - elasticroles
      - rolemappings
      - clustersettings
      - ingestpipelines
      - repos
      - apikeys
      - componenttemplates
      - legacyindextemplates
      - watchers
      - scripts
      - transforms
      - dataviews
      - connectors
      - alertrules
    verbs: ["get","watch","list" ]
