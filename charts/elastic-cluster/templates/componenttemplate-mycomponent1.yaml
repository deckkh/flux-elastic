{{ if  .Values.componenttemplates.mycomponent1 }}
apiVersion: elastic.elastic-operator/v1
kind: ComponentTemplate
metadata:
  name: {{ .Release.Name}}-componenttemplate-mycomponent1
  namespace: observability
spec:
  name: mycomponent1
  cluster: {{ .Release.Name}}
  body: |
        {
          "template": {
            "mappings": {
              "properties": {
                "@timestamp": {
                  "type": "date"
                }
              }
            }
          }
        }
{{ end}}            